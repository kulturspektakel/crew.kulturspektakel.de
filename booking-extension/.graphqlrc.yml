schema: https://api.kulturspektakel.de/graphql
documents:
  - ./src/booking/**/*.ts
  - ./src/booking/**/*.tsx
generates:
  ./src/booking/types/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
  ./src/booking/types/graphql.schema.json:
    plugins:
      - introspection
    config:
      minify: true
config:
  withMutationFn: true
  scalars:
    Date: Date
    DateTime: Date
hooks:
  afterOneFileWrite:
    - prettier --write
